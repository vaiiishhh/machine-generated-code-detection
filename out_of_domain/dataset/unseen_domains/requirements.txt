# CoDet-M4 Unseen Domains Pipeline v2
# Install with: pip install -r requirements.txt
#
# Note: vLLM must be installed separately first as it pulls in
# its own pinned versions of torch and transformers.
# Recommended install order:
#   1. pip install vllm
#   2. pip install -r requirements.txt

# ── Inference engine ──────────────────────────────────────────
vllm>=0.4.0

# ── Model loading & tokenization ─────────────────────────────
transformers>=4.43.0        # Qwen2.5 requires >= 4.37; 4.43+ for Llama-3.1
tokenizers>=0.19.0
accelerate>=0.30.0          # device_map and multi-GPU helpers
huggingface-hub>=0.23.0     # model download / caching

# ── Datasets ─────────────────────────────────────────────────
datasets==2.18.0          

# ── Data handling ─────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0

# ── PyTorch (B200 / CUDA 12.x) ───────────────────────────────
# vLLM installs its own torch; only add this if you need an explicit version.
# torch>=2.3.0   # uncomment if installing torch independently

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.66.0                # progress bars inside datasets/transformers
sentencepiece>=0.2.0        # tokenizer for CodeLlama and some Qwen models
protobuf>=3.20.0            # required by sentencepiece
einops>=0.7.0               # used by some model architectures (DeepSeek)